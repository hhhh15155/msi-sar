# YRD vs YRD2509 分类精度差异分析与求解

## 背景

两个数据集均在黄河三角洲区域，使用 Sentinel-2 (10波段) + Sentinel-1 SAR 影像进行地物分类。

| | YRD | YRD2509 |
|---|---|---|
| 空间分辨率 | 相同 (10m) | 相同 (10m) |
| 影像尺寸 | 2048×2048 | 1024×1024 (YRD的子区域) |
| 总通道数 | **14** | **12** |
| 光学通道 | 10 (B2-B12) | 10 (B2-B12) |
| SAR通道 | **4** (VV线性, VH线性, 比值特征, 纹理特征) | **2** (VV-dB, VH-dB) |
| 类别数 | 8 | 9 |
| 标注方式 | 人工标注 | 土地利用分类图(分类器产出+人工验证) |
| 标注像元数 | 244万 | 58万 |

## 重叠地物类别

两者共有的地物（同一区域、同一物种）：Tamarix(柽柳)、Suaeda(碱蓬)、Reed/Phragmites(芦苇)、Willow(柳树)、Tidal flat(潮滩)、Water/River+Pond(水体)。

## 100-shot 分类精度 (OA%)

| 模型 | YRD(8类) | YRD2509(9类) |
|------|---------|-------------|
| mghofnet | 93.3 | 78.0 |
| dfinet | 92.4 | 76.3 |
| frekfuse | 90.3 | 85.1 |
| softformer | (跑中) | ~67 (fs5) |

## 核心发现：混淆类对特征可分性分析

对最关键的混淆对 **Tamarix ↔ Suaeda**，在归一化特征空间中的类中心距离：

```
               光学距离(10波段)   SAR距离   联合距离   信噪比(类间/类内)
YRD (14ch)         2.50          1.03      2.70        5.50
YRD2509 (12ch)     0.40          0.26      0.48        1.09
YRD/YRD2509        6.3x          4.0x      5.6x        5.0x
```

同样分辨率、同样传感器、同一区域的同两种植物——YRD2509 的近红外波段(B8)差异只有 YRD 的 1/11，B8A 差异只有 1/142。

## 已尝试的方案

1. **从 VV/VH 构造 SAR 派生特征** (VH/VV比值, RVI, NDPI, 局部纹理): 
   - Tamarix-Suaeda SAR距离: 0.26→0.29 (+12%)，杯水车薪
   - Reed-Willow SAR距离: 0.15→0.37 (+147%)，有效但远不及 YRD 的 1.07
   - 构造特征与光学波段 R²<0.1，确是独立信息，但原料局限于 VV/VH 两个数，无法凭空创造 YRD 中那两个独立物理来源的 SAR 通道

2. **增大 batch_size、增加并发训练**：工程优化，不改变数据本质

## 待求解的问题

1. **6倍光学距离差距的根因是什么？** 同样的传感器、同样的分辨率、同样的地物，为什么 YRD2509 的近红外对 Tamarix 和 Suaeda 几乎无区分力？是标签噪声（分类图边界的混合像元）还是大气校正/预处理差异？

2. **在不重新标注的情况下，如何缩小这个差距？** 可能的思路：
   - 标签清洗：剔除分类边界/低置信度像元作为训练样本
   - 对比学习/度量学习：让模型自己学到有区分力的嵌入，减轻标签噪声影响
   - 半监督：用 YRD 的模型在 YRD2509 上做伪标签蒸馏
   - 数据增强：mixup/cutmix 对边界混合像元可能有帮助
   - SAR-specific 增强：利用 Sentinel-1 的多时相特性（如果有不同日期数据）

3. **YRD 的那 2 个额外 SAR 通道能否通过外部数据补回？** 例如 Sentinel-1 的相干性(coherence)、InSAR 相位、不同极化分解（Freeman-Durden, H-α）等。

4. **frekfuse 在 YRD2509 上领先其他模型 7-9% OA (85.1 vs 76-78)，说明频域特征对标签噪声更鲁棒？** 是否可以从模型架构角度进一步优化？
